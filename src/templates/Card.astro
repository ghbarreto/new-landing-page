---
const { data } = Astro.props;
---

<div class="d-flex justify-center">
    <div class="bg-white dark:bg-slate-500 transition-colors">{data.name}</div>
    <img src={data.avatar_url} alt={data.name} />
    Location: {data.location}
</div>
