---
import { Text } from '../components/Text';
import Links from '../templates/Links.astro';

const center = 'w-4/5 mx-auto mt-2 mobile:w-5/6 mobile:flex mobile:justify-center';

const { data } = Astro.props;
---

<section class="mt-5">
    <div class="flex mobile:block">
        <div class="w-3/6 mobile:my-0 mobile:mx-auto mobile:w-1/6">
            <img
                src={data.avatar_url}
                alt={data.name}
                class="my-0 mx-auto rounded-full w-72 border-2 border-highlight_300 shadow-lg shadow-highlight_300 dark:border-lightblue_600 dark:shadow-lightblue_600"
            />
        </div>

        <div class="mt-5 text-left">
            <span class="flex justify-center">
                <Text client:load text="hello" type="header" />
            </span>

            <span class={`flex items-center mt-5 ${center}`}>
                <Text text="whoami" type="body" />
                <Text type="bold" text={data.name} customClasses="ml-1" />
            </span>

            <span class={`flex items-center ${center}`}>
                <Text text="about" type="body" customClasses={center} />
                <Text text="company" type="link" href="https://fansunite.com/" customClasses={`${center} mr-5`} />
            </span>
            <Text text="role" customClasses={center} type="body" />
            <!-- <Text  text="" type="link" href="https://fansunite.com/" /> -->
            <Text text="" customClasses="ml-1" type="body" />
            <div class="flex justify-center mt-5 mb-20 mt-10 gap-5 mobile:mb-3">
                <Links title="Linkedin" href="https://www.linkedin.com" icon="gg-work-alt" />
                <Links title="More Info" href="https://www.linkedin.com" icon="gg-work-alt" />
                <Links title="Resume" href="https://github.com" icon="gg-file-document" />
            </div>
        </div>
    </div>
</section>
